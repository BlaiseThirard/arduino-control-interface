#ifndef ARDUINO_H
#define ARDUINO_H

#include <QObject>

class Arduino : public QObject
{
    Q_OBJECT
public:
    explicit Arduino(QObject *parent = 0);
    
signals:
    
public slots:
    
};

#endif // ARDUINO_H
